<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bnk.expense.ExpenseMapper">

    <!-- 경비집행내역 단건 조회 (사용중인 건만) -->
    <select id="selectExpense" resultType="ExpenseExec">
        SELECT EXEC_NO, DEPT_CD, AMOUNT, STATUS, USE_YN
        FROM TB_EXPENSE_EXEC
        WHERE EXEC_NO = #{execNo}
          AND USE_YN = 'Y'
    </select>

    <!-- 경비집행내역 등록 (초기 상태 R:등록) -->
    <insert id="insertExpense">
        INSERT INTO TB_EXPENSE_EXEC (EXEC_NO, DEPT_CD, AMOUNT, STATUS, REG_USER)
        VALUES (#{execNo}, #{deptCd}, #{amount}, 'R', #{regUser})
    </insert>
</mapper>
